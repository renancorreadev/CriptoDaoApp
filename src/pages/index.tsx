import Head from "next/head";
import type { NextPage } from "next";
import { Navbar } from "@components/Interface";

const Home: NextPage = () => {
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default Home;
